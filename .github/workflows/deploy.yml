name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy app
        run: |
          cd ~/catty-reminders-app
          git pull origin main
          bash deploy.sh
