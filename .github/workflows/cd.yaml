name: CD

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy
        run: |
          chmod +x ./deploy.sh
          ./deploy.sh ${{ github.sha }}
